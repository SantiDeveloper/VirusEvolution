package virusexpansion;

import java.io.File;

/**
 *
 * @author Santiago Pérez Gómez
 */
public class FicheroUtils {

    
    /**
     * Método que lee el fichero de texto con los parámetros de entrada y los carga
     * en un objeto de tipo Simulacion.
     * 
     * @param pathFile : Ruta con el fichero de texto que contiene los parámetros 
     * para ejecutar la simulación
     * @param simulacion : estructura donde se va a almacenar la información obtenida del fichero.
     */
    public static void leerFichero(String pathFile, Simulacion simulacion){
        
    }
    
    /**
     * Método que a partir de un objeto de tipo Simulacion, rellena una tabla con 
     * la información de dicho objeto.
     * 
     * @param resultado : se reciibe por parámetro la información calculada
     * en la ejecución de la simulación.
     * @return File: Se recibe el fichero con la tabla generada con los resultados
     * de la simulación.
     */
    public static File generarTabla(Simulacion resultado){
        
        return null;
    }
}
